import { App.Sqrt.TsPipe } from './app.sqrt.ts.pipe';

describe('App.Sqrt.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new App.Sqrt.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
